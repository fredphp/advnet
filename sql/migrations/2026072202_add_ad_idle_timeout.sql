-- ============================================================
-- 广告空闲超时 - 数据库变更
-- 功能：用户在红包群页面不操作超过指定秒数后，信息流广告不再计费
-- 执行前请备份数据库
-- ============================================================

-- 1. 在广告配置分组中新增"广告空闲超时"配置项
INSERT INTO `advn_config` (`name`, `group`, `title`, `tip`, `type`, `visible`, `value`, `content`, `rule`, `extend`, `setting`) VALUES
('ad_idle_timeout', 'ad', '广告空闲超时(秒)', '用户在红包群页面不触摸屏幕超过此时间后，信息流广告不再累计金币（防止挂机刷广告风险），设置为0则不限制', 'number', '', '30', '', 'required|integer|gte:0', '', '')
ON DUPLICATE KEY UPDATE `group` = VALUES(`group`), `title` = VALUES(`title`), `tip` = VALUES(`tip`);

-- ============================================================
-- 执行后操作：
-- 1. 清除后台缓存：rm -rf runtime/cache/*
-- 2. 在后台 → 常规管理 → 系统配置 → 广告配置 中可以看到新增的"广告空闲超时"
-- 3. 工作原理：
--    a) 前端追踪用户触摸/滚动操作，记录最后活跃时间
--    b) 每5秒检查一次，超过 ad_idle_timeout 秒未操作 → 标记为不活跃
--    c) 不活跃时，信息流广告不再调用 recordView API，不累加 ad_freeze_balance
--    d) 用户恢复操作后，自动恢复计费
--    e) 不封号，仅暂停计费，用户体验友好
-- ============================================================
